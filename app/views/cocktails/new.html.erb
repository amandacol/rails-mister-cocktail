<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <h1>Monte sua própria receita</h1>

      <%= simple_form_for @cocktail do |f| %>
        <%= f.input :name %>
        <%= f.input :photo, as: :file %>
        <%= f.button :submit, "Novo Drink", class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>
